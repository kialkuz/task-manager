Описание проекта

Разработан REST API сервиса для управления задачами.

Задачи:
– проектирование архитектуры backend-сервиса
– реализация REST API (CRUD операции, бизнес-логика)
– работа с PostgreSQL
– реализация middleware
– реализация механизма обработки задач по заданным условиям

Функциональность:
– управление задачами с изменением статусов (открытие/закрытие)
– механизм автоматического изменения параметров задач по заданным правилам
– возможность настройки времени выполнения задач (по дням недели, месяцу, году)
– запуск обработки задач через API endpoint

Особенности реализации:
– слоистая архитектура (handlers / services / repository)
– разделение бизнес-логики и слоя доступа к данным
– централизованная обработка ошибок
– подготовка структуры проекта к расширению

Техническое описание проекта

В директории `tests` находятся тесты для проверки API, которое должно быть реализовано в веб-сервере.

Директория `web` содержит файлы фронтенда.

Данный проект представляет собой менеджер задач позволяющий добавлять, редактировать, удалять задачу.
При добавлении/редактировании задачи действуют правила вычисления следующей даты работы с задачей.
После выполнения задачу нужно отметить выполненной. Если она была создана без правил повторения,
то переносится на следующую дату. Если нет - просто удаляется.

Для запуска проекта нужно:
1. скомпилировать приложение командой go build -o app.exe main.go
2. запустить ./app.exe

Для работы проекта используются следующие переменные среды в env файле:
Путь к веб директории
WEB_DIR_PATH: web
Порт запуска
PORT: "7540"
Путь к бд
DB_FILE: "scheduler.db"
Максимальное количество дней для переноса задачи
MAX_INTERNAL_DAYS: 400

Для запуска тестов использовать команду go test ./tests
При запуске тестов используются параметры в файле tests/settings.go
Параметры FullNextDate, Search можно включить. Реализован весь функционал.

Аутентификация реализована. Для тестирования добавить в tests/settings.go
значение: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.e30.396KCDWMomWrMEImsF84AmFRjBEvSvnyLh3ZA_mB_Wg
